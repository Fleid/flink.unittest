pyyaml>=6.0
duckdb>=0.9.0

# Optional: install for SQL lint checks (--lint flag)
# sqlglot>=20.0

# Optional: install for Parquet fixture files (rows_file: *.parquet)
# pyarrow>=14.0

# Optional: install for Avro fixture files (rows_file: *.avro)
# fastavro>=1.9.0

# Optional: install for real Flink SQL semantics (streaming windows, watermarks)
# apache-flink>=1.18.0
